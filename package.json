{
  "name": "poc-apollo-federation",
  "version": "1.0.0",
  "description": "",
  "main": "gateway.js",
  "scripts": {
    "server": "concurrently -k npm:server:*",
    "server:rest": "json-server -q db.json",
    "server:resource": "node resource.js",
    "server:activity": "node activity.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/federation": "^0.20.0",
    "@apollo/gateway": "^0.20.0",
    "apollo-server": "^2.17.0",
    "concurrently": "^5.3.0",
    "graphql": "^15.3.0",
    "json-server": "^0.16.1",
    "node-fetch": "^2.6.1"
  }
}
